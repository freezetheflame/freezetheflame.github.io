# User Preferences

- Prefer Chinese collaboration and summaries by default.
- The wiki should be usable across multiple computers through GitHub sync.
- Public repo memory is acceptable when it contains project-level, non-sensitive context.
- Private data separation can be added as a later layer.
